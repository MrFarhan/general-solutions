# React Native

Dynamic adjustment of image on all devices (Android & IOS)
```
 width: "100%",
 height: (Dimensions.get('screen').width / 1.7) * 0.692,
```

Create React Native Project with specific version:
```
npx @react-native-community/cli@latest init your_app_name --version 0.76.6
```
